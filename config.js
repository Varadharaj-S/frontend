/*
config.js — the ONLY file you should need to edit when your backend
(Vercel) or frontend (Cloudflare Pages) domain changes. No other file
hardcodes a domain name; everything reads window.BASE_API_URL from here.
*/
const BASE_API_URL = "daily-submission-tracker-ciyc.vercel.app";

window.BASE_API_URL = BASE_API_URL;
